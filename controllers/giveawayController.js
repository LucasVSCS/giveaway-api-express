const giveawayModel = require('../models/giveaways')
const apify = require('apify-client')

const giveawayController = {
  newGiveaway (req, res) {
    //https://api.apify.com/v2/acts/shu8hvrXbJbY3Eb9W/runs?token=zpQGeSWAZwfnvcr8A4xG5AFAB
    /*
    userModel.getUsers((err, data) => {
      try {
        if (err) {
          res.status(500).send(err)
        } else if (data) {
          res.send(data)
        }
      } catch (error) {
        res.status(500).send(error)
      }
    })

  */
  }
}

module.exports = giveawayController
